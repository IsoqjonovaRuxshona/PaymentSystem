package org.example.controller;

import org.example.model.Card;

import java.util.ArrayList;

import static org.example.Main.cardService;
import static org.example.Main.scanStr;

public class TrasferController {
    public static void p2p() {
      /*  System.out.print("Enter receiver number: ");
        String receiver = scanStr.nextLine();

        Card receiverCard = cardService.getCardByNumber(receiver);
        if (receiverCard == null) {
            System.out.println("Card not found");
            return;
        }

        System.out.print("Enter amount: ");
        double amount = scanInt.nextDouble();

        ArrayList<Card> cards = CardCantroller.read();
        System.out.print("Choose card: ");
        int choice = scanInt.nextInt() - 1;


        if (transactionService.transferP2P(receiverCard, cards[choice], amount) == 1) {
            transactionService.add(new TransactionController(cards[choice].getId(), receiverCard.getId(), amount));
            System.out.println("success");
        } else {
            System.out.println("Not enough funds");
        }

    }*/
    }
}
